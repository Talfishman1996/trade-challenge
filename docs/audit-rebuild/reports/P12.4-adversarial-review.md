# P12.4 Adversarial Review

Evidence ID: `EV-P12-ADV-004`

The adversarial pass intentionally attempted invalid, repeated, interrupted,
destructive, stale, and extreme actions. It found defects; none were hidden.

| Attack | Initial result | Disposition |
|---|---|---|
| compare implementation line-by-line with P10 | detailed/autosave/filter gaps | reopened G11; repaired in `EV-P11-REPAIR-007` |
| reconnect after offline state update | stale closure retried as offline | repaired with forced-online sync path |
| empty close timestamp | possible `toISOString` throw | validation/focus containment plus unit test |
| rapid submit/autosave+Save | duplicate risk | synchronous guard and dirty revision check |
| negative BE | accepted without explicit cost acknowledgement | required acknowledgement added |
| review completion during persistent drawdown | remained permanently due | due now requires unreviewed evidence |
| review note | visible but discarded | note required and persisted with review receipt |
| Undo after eight seconds | stale visible action/silent no-op | Undo expires; System offers prototype-only tombstone recovery |
| P&L below zero equity | model returned terminal but UI formatted `$0` | explicit no-active-recommendation state |
| three-button capture footer | visual horizontal scrollbar | fixed; measured sheet/form overflow now zero |
| 500 records | possible unbounded DOM | 30 cards/661-node measured initial document; targeted search returns one |
| sync error/conflict | false success risk | retry remains failed; conflict requires explicit choice then check |
| malformed fixture | fabricated zero risk | full bounded recovery, no app canvas until reset |

Residual prototype limitations are intentional and visible: native confirm/alert is
used for discard/reset/preview ceremony; network/import/export/media are simulated;
localStorage eviction was source-reviewed but not browser-injected; physical mobile
platform behavior is unverified. These are not production-ready claims.
