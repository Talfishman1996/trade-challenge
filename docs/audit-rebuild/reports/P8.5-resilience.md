# P8.5 Resilience

Captured: 2026-07-12 PDT

State: `EVIDENCE_READY`

Evidence ID: `EV-P8-RES-005`.

| Failure/interrupt | Actual result |
|---|---|
| offline save | local equity/count update and honest local-save toast |
| reconnect | status becomes Ready; no immediate sync |
| 2.5 s/request save | local state updates by 100 ms; sheet locked until ~5.3 s |
| forced 503/manual sync | stale Synced label remains |
| daily-loss threshold | uncaught `fmt` ReferenceError blanks the entire app |
| concurrent devices | duplicate ID/key warning and ambiguous mutation target |
| explicit delete | irreversible; adjacent Undo targets another record |
| remote-only image key | missing binary on the second device |
| malformed local JSON/storage exception | silently falls back to normalized empty/best-effort state |
| malformed/missing/error cloud | collapses to `null`; may trigger unsafe bootstrap path |
| reload/background/keyboard/import | genuine mobile evidence unavailable |

There is no durable idempotency key, outbox, top-level recovery boundary, transactional
storage acknowledgement, or diagnostic recovery screen. A route ErrorBoundary cannot
catch an App-level gate crash, and its generic retry does not preserve or explain the
failed operation.

The current app fails the reliability gate. Prototype validation must inject local
quota/transaction failure, auth, 404, conflict, 413, 429, 5xx, timeout, invalid
payload, duplicate submit, reload during each mutation state, and interrupted media.
