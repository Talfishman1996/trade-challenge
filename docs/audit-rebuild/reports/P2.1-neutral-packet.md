# P2.1 Neutral Packet Construction

Captured: 2026-07-12 PDT

State: `EVIDENCE_READY`

## Packet

The independent-review packet is generated at:

`output/audit-rebuild/fable5/packet-v1/`

Its tracked authored inputs are under:

`docs/audit-rebuild/fable5/packet-v1/`

## Composition

| Item | Count |
|---|---:|
| Manifested artifacts | 172 |
| Unannotated JPEG screenshots | 71 |
| Tracked application/Worker/build files | 40 |
| Origin-matched source/evidence artifacts | 169 |
| Authored factual/prompt files | 3 |
| Bytes represented by manifest | 22,054,187 |
| Prior finding/gate/report/control documents | 0 |
| Workflow videos | 0; capability gap stated in packet |

The packet contains the complete unannotated P1 screenshot corpus, deterministic
fixtures, raw metric/source/data/surface extracts, and a Git-generated snapshot of
the tracked application and Worker source. It excludes the findings registry, P1
reports, gate verdicts, plans, decision logs, and Fable protocol.

## Current and Planned Model Separation

`FACTS.md` identifies the current tracked `$20K` two-thirds-power implementation
separately from the planned `$100K` PCHIP sizing intent. It supplies the seven
approved anchors and fixed planning inputs without presenting an implementation
verdict or preferred interface treatment.

## Integrity

Manifest file:

`output/audit-rebuild/fable5/packet-v1/ASSET-MANIFEST.tsv`

Manifest SHA-256:

`9c100834710956cba37097ddf7a92b27def19f01c86c1899fe94460d3e4f18c9`

Every manifest row passed path, byte-count, and SHA-256 verification. All 169 copied
source/evidence rows match their Git or frozen P1 origin. Incidental macOS `._*`
sidecars are not manifested and the prompt requires the reviewer to inspect only
manifested artifacts.

## Privacy Boundary

The packet contains synthetic trade fixtures and no production trade payload,
credential, private sync record, or user password. The tracked public Worker
hostname remains present because it is part of the application source contract.
The review command will expose no parent-directory review documents.

## Transition

P2.1 is evidence-ready. P2.2 must independently audit authored language, packet
paths, execution flags, and content hashes before any model execution is allowed.
