# P5.4 Rendering and Delivery

Captured: 2026-07-12 PDT

State: `EVIDENCE_READY`

Evidence ID: `EV-P5-PERF-004`.

## Current Delivery

- Production output is 9.132 MiB logical with an 810.39 kB main JS chunk.
- Two public monument rasters total about 8.56 MB, while Home embeds another base64
  JPEG and remains eager in the main graph.
- Lazy loading exists for secondary destinations and overlays, but Home and its
  full monument subsystem are startup-critical.
- 500 history records mount 8,185 elements with no scale boundary.
- Recharts emits invalid-container-dimension warnings during transitions.
- Google-hosted fonts add external dependency; chart code asks for an unloaded font.

## Target Budgets

| Budget | Prototype target |
|---|---:|
| initial JS gzip | <= 170 kB |
| initial CSS gzip | <= 20 kB |
| initial image transfer | <= 350 kB responsive AVIF/WebP |
| route chunk gzip | <= 90 kB each |
| visible journal rows | bounded window <= 60 DOM records |
| runtime console | zero errors/warnings in W01-W17 |
| mobile LCP local-throttled target | <= 2.5 s |
| CLS | <= 0.1 |

Separate a lightweight operational Home shell from the optional expanded monument.
Use responsive images, remove duplicate/embedded raster copies, lazy-load chart
libraries by insight panel, and render lists through pagination/windowing. Chart
frames must reserve dimensions before mount.
